<% if (user) { %>
    <h1>Hello <%= user.displayName %></h1>
    <p><%= user.email %> <%= user.googleId %></p>
    <a href="/auth/logout">Logout</a>
<% } else { %>
    <h1>Welcome to the app</h1>
    <a href="/auth/google">Login with Google</a>
<% } %>